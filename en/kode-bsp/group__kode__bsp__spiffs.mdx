---
title: SPIFFS
summary: Mount/unmount the on-flash SPIFFS partition. 

---

# SPIFFS

Mount/unmount the on-flash SPIFFS partition. 

## Functions

|                | Name           |
| -------------- | -------------- |
| esp_err_t | **[kode_spiffs_mount](/en/kode-bsp/group__kode__bsp__spiffs#function-kode-spiffs-mount)**(void )<br>Mount the SPIFFS partition.  |
| esp_err_t | **[kode_spiffs_unmount](/en/kode-bsp/group__kode__bsp__spiffs#function-kode-spiffs-unmount)**(void )<br>Unmount the SPIFFS partition.  |


## Functions Documentation

### function kode_spiffs_mount

```
static inline esp_err_t kode_spiffs_mount(
    void 
)
```

Mount the SPIFFS partition. 

### function kode_spiffs_unmount

```
static inline esp_err_t kode_spiffs_unmount(
    void 
)
```

Unmount the SPIFFS partition. 





-------------------------------

Updated on 2026-05-13 at 15:31:21 +0000